# The `arch` module can be accessed with `from pwndbg.gdb.arch import arch_mod`,
# while `pwndbg.gdb.arch` will represent the `Arch` object
from pwndbg.gdb import arch as arch_mod
from pwndbg.gdb.arch import arch
